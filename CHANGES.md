# Changelog

## [Unreleased]

### Changed
- **Docker Compose V2**: Updated all scripts and documentation to use `docker compose` instead of `docker-compose`.
- **Scripts**: Updated `backup.sh` and `restore.sh` to use the new syntax.
- **Documentation**: 
    - Updated `README.md` with V2 instructions and version check.
    - Added `DOCKER_COMPOSE_V2.md` migration guide.
    - Added `compose.sh` compatibility wrapper.
